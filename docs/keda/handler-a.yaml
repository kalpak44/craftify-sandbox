apiVersion: batch/v1
kind: Job
metadata:
  name: handler-a
  namespace: userspace
spec:
  template:
    metadata:
      labels:
        job: handler-a
    spec:
      restartPolicy: Never
      containers:
        - name: busybox
          image: busybox:1.35
          command: ["sh", "-c", "echo Handling EVENT_A && sleep 5"]
